name: Pull Request

on:
  pull_request:
    branches:
      - main

jobs:
  ci:
    uses: oxHive/pipelines/.github/workflows/rust-check.yml@main
    with:
      upload-coverage: true
    secrets: inherit
