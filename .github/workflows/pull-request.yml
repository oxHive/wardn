name: Pull Request

on:
  pull_request:
    branches:
      - main

jobs:
  ci:
    uses: oxHive/pipelines/.github/workflows/rust-check.yml@v2
    with:
      upload-coverage: true
    secrets: inherit
